{
  "name": "server",
  "version": "1.0.0",
  "description": "Websocket server handling connections to the rooms and communication in the rooms",
  "type": "module",
  "scripts": {
    "start": "node dist/index.js",
    "dev": "concurrently \"npx tsc -w\" \"nodemon -q dist/index.js\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.20",
    "@types/node": "^20.8.9",
    "concurrently": "^8.2.2",
    "nodemon": "^3.0.1",
    "tsconfig": "workspace:*",
    "typescript": "^5.2.2"
  }
}
